# Paths
features_dir: /mnt/cephfs/mir/jcaicedo/morphem/dataset/study_replication/idr0017/subcell_mae_n_1_p_2_unmasked_features/ # feature embedding path
metadata_path: "/mnt/cephfs/mir/jcaicedo/morphem/dataset/metadata/single-channel/v8/idr0017_meta.csv" # metadata csv path
save_dir: /scr/vidit/idr17_scores # directory to save analysis results

# Model and Benchmark Configuration
model_name: subcell_unmasked_features # for directory naming
benchmark_test: auc_roc # auc_roc, replicate_corr
fusion_type: early_fusion # late_fusion, early_fusion
mutations: null # null or list of mutations as per metadata

# Distance Metrics
distance_metric: euclidean # euclidean, cosine





